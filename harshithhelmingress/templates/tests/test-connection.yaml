apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "harshithhelmingress.fullname" . }}-test-connection"
  labels:
    {{- include "harshithhelmingress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "harshithhelmingress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
